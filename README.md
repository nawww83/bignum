# Библиотека целых иерархических чисел разрядности кратно 128 бит

## Поддерживаемые типы

* Базовый беззнаковый тип U128 (поведение аналогично беззнаковым типам языка С++, например, uint64_t). Нет оверхеда.
* Знаковый тип I128, базирующийся на U128, с поддержкой inf/nan. Оверхед на знак и на признак сингулярности.
* Беззнаковый тип UBig, который строится на основании U128 иерархически, удваивая свой размер на каждом шаге. Нет оверхеда.

Под иерархичностью чисел подразумевается возможность конструировать числа требуемой разрядности кратно степени 2, начиная со 128 бит:

using U128 = u128::U128;
using U256 = UBig<U128>;
using U512 = UBig<U256>;
...

Таким образом, разрядность хоть и ограничена, но может быть достаточно большой. Также можно при необходимости по аналогии добавлять поддержку I256, I512 и т.п.

Для платформ и компиляторов со встроенной поддержкой 128-бит (например, GCC/Clang) используются нативные интринсики. Также поддерживаются интринсики от MSVC.

Для экзотических платформ реализованы "ручные" алгоритмы. Это, например, авторское деление (с доп. подсказками ИИ), а также оригинальное умножение чисел с расширением разрядности (mult_ext). Остальные операции достаточно тривиальные.

## Стандарт 

Не ниже C++20.

## Плюшки

* Встроен литерал _u128 (для U128).
* Имеется конвертация "из строки"/"в строку" (полностью подсказка ИИ).
* Поддержка numeric_limits, а также хеширования для соответствующих контейнеров (для U128).
* Функции countl_zero(), countr_zero(), popcount() и bit_width().

## Дополнения для 128-битных чисел 

Реализованы целочисленные алгоритмы:

* Вычисление квадратного корня.
* Вычисление корня n-й степени.
* Некоторые модулярные операции (экспериментально).

## Где используется

Калькулятор с числами типа "Decimal", с поддержкой факторизации целых чисел, https://github.com/nawww83/calculator_qml_decimal
